var express = require('express');
var router = express.Router();
var productController = require('../controllers/productsController');

//request, response
router.get('/', productController.indexAction);

// router.get('/products', function(request, response){
// 	var productsId=request.query.id;
// 	var token=request.query.token;

// 	response.send(productsId + ' '+token);
// });

//regex accept only number      /:firstName/:lastName
router.get('/:id(\\d+)', productController.showAction);

router.get('/:id(\\d+)/delete', productController.deleteAction);


// // regex ? is 0 or 1
// router.get('/ab?cd', function(request, response){
// 	response.send('une zolie requete');
// });

// // anything ends with fly
// router.get(/.*fly$/, function(request, response){
// 	response.send('une zolie requete');
// });

router.post('/', productController.insertAction);


module.exports = router;