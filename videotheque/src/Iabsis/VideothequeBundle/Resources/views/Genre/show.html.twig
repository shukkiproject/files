{% extends '::base.html.twig' %}

{% use 'IabsisVideothequeBundle:Admin:sidebar_admin.html.twig' %}

{% block body -%}
    <h1>Détail du genre</h1>

    <table class="record_properties table table-bordered table-hover records_list">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Label</th>
                <td>{{ entity.label }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-default" href="{{ path('genre') }}">
        Back to the list
    </a>

    <a class="btn btn-primary" href="{{ path('genre_edit', { 'id': entity.id }) }}">
        Edit
    </a>

    {{ form(delete_form, { 'attr': {'class': 'delete-form'} }) }}

{% endblock %}
